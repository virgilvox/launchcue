[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  external_node_modules = ["jsonwebtoken", "mongodb", "bcryptjs", "zod"]

# Default SPA redirect is often handled automatically by Netlify Dev
# Try removing explicit [dev] block and redirect
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200 